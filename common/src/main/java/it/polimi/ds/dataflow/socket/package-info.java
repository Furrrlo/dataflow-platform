/**
 * Shared package which contains the implementation for socket-to-socket communication protocol
 * which will be used by both the client and the server.
 * <p>
 * Additionally, the {@link it.polimi.ds.dataflow.socket.packets} subpackage contains all the possible packets that can
 * be sent and received.
 */
@NullMarked
package it.polimi.ds.dataflow.socket;

import org.jspecify.annotations.NullMarked;